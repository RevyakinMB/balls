{
  "name": "balls",
  "version": "0.0.1",
  "description": "spheres impact simulation",
  "main": "app.js",
  "scripts": {
    "start": "http-server ./app -a localhost -p 8000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.11.1"
  }
}
